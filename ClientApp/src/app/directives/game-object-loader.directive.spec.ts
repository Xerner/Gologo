import { GameObjectLoaderDirective } from './game-object-loader.directive';

describe('GameObjectLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new GameObjectLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
